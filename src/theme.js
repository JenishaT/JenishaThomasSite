import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "rgb(68, 164, 241)"
    },
    secondary: {
      main: "rgb(22,67,95)"
    }
  }
});

export default theme;
